class Brand {
  constructor(rawInput) {
    this.name = rawInput.name;
    this.link = rawInput.link;
    this.colors = rawInput.colors;
    this.logo = rawInput.logo;
    this.distraction = null;
  }
}

export default Brand;
